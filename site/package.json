{
  "name": "site",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^0.12.1"
  },
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "deploy": "yarn build && npx netlify-cli deploy --prod"
  }
}
